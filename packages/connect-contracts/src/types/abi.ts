/**
 * ABI Type Definitions for Klever Smart Contracts
 *
 * Exported from @klever/connect-encoding for use in connect-contracts.
 * Defines the structure of contract ABIs, endpoints, parameters, and types.
 */
import type {
  ContractABI,
  BuildInfo,
  ABIEndpoint,
  ABIParameter,
  FieldDefinition,
  VariantDefinition,
  ABITypeDefinition,
} from '@klever/connect-encoding'

export type {
  ContractABI,
  BuildInfo,
  ABIEndpoint,
  ABIParameter,
  FieldDefinition,
  VariantDefinition,
  ABITypeDefinition,
}
