{
  "name": "@klever-examples/common-tx-fetch-by-hash",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Look up a transaction by hash, render its fields, build the explorer URL.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run --exclude \"**/*.testnet.*\"",
    "test:testnet": "vitest run src/tx-fetch-by-hash.testnet.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@klever/connect": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
