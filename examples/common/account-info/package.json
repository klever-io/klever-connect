{
  "name": "@klever-examples/common-account-info",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fetch full account: nonce, all asset balances, frozen amounts, permissions.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:testnet": "vitest run src/account-info.testnet.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@klever/connect": "workspace:*" },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
