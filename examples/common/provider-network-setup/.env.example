# Optional: override the default network. One of: mainnet | testnet | devnet | local
KLV_NETWORK=testnet

# Optional: a custom RPC endpoint to demonstrate createCustomNetwork()
# Example: https://my-private-node.example.com
KLV_CUSTOM_RPC=
