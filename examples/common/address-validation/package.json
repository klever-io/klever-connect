{
  "name": "@klever-examples/common-address-validation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Validate user-input Klever addresses (regex + bech32 decode).",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:testnet": "vitest run src/address-validation.testnet.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@klever/connect": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
