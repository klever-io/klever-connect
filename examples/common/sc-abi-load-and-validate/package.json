{
  "name": "@klever-examples/common-sc-abi-load-and-validate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Load an ABI JSON, validate it, list the endpoints / events.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:testnet": "vitest run src/sc-abi-load-and-validate.testnet.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@klever/connect": "workspace:*",
    "@klever/connect-contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
