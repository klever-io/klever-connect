{
  "name": "@klever-examples/common-block-fetch",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fetch a block by number or 'latest'; walk its transactions.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:testnet": "vitest run src/block-fetch.testnet.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@klever/connect": "workspace:*" },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
