{
  "name": "@klever-examples/common-key-pair-generate-and-import",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Generate Ed25519 key pairs, import from hex, derive public keys.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:testnet": "vitest run src/key-pair-generate-and-import.testnet.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@klever/connect": "workspace:*" },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
