# Network selection - testnet by default
VITE_NETWORK=testnet

# Optional: a marketplace ID to list against. If absent, the form prompts for it.
# (Find one via the explorer or your own marketplace creation flow.)
VITE_MARKETPLACE_ID=

# DO NOT add VITE_DEV_PRIVATE_KEY. Production runtime uses the Klever Web Extension.
