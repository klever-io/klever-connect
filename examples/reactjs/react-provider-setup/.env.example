# Vite environment variables — copy this to `.env.local` and tweak.
# All variables exposed to the browser MUST be prefixed `VITE_`.

# Network the example connects to. One of: mainnet | testnet | devnet | local.
# Defaults to "testnet" if unset.
VITE_KLV_NETWORK=testnet
