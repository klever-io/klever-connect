// Adds @testing-library/jest-dom matchers (toBeInTheDocument, toHaveTextContent, …)
// to vitest's `expect`. Imported once via vitest config's `setupFiles`.
import '@testing-library/jest-dom/vitest'
