# Expected output — `undelegate`

```
Klever Connect — Undelegate

Connected as klv1abcd…xyz. Undelegating leaves the bucket frozen — start
the cooldown via unfreeze/ next.

Bucket id   [ 0x...               ]

[ Undelegate ]
```

After signing in the extension popup:

```
✓ Undelegated! Hash: 0x...
```
