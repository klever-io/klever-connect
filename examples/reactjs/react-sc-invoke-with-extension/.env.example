VITE_NETWORK=testnet
# Counter contract address.
VITE_COUNTER_ADDRESS=
# Where to fetch the ABI JSON from. Defaults to /counter.abi.json (served from public/).
VITE_ABI_URL=/counter.abi.json
# DO NOT add private keys.
