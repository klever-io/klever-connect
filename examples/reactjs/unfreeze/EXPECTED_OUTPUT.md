# Expected output — `unfreeze`

```
Klever Connect — Unfreeze

Connected as klv1abcd…xyz. Unfreezing starts the cooldown period — you
cannot withdraw until it elapses (network-configured).

KDA              [ KLV               ]
Bucket id        [ <32-byte hex>     ]

[ Unfreeze KLV ]
```

After signing in the extension popup:

```
✓ Cooldown started. Hash: 0x...
```

For a non-KLV KDA the bucket-id field is hidden.
