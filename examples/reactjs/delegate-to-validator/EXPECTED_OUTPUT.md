# Expected output — `delegate-to-validator`

```
Klever Connect — Delegate

Connected as klv1abcd…xyz. Delegate a frozen KLV bucket to a validator
to start earning rewards.

Validator (klv1…)  [ klv1...                ]
Bucket id          [ <hex hash>             ]

[ Delegate ]
```

After signing in the extension popup:

```
✓ Delegated! Hash: 0x...
```

Leave the bucket id blank to let the SDK create one automatically.
