{
  "name": "@klever/example-nodejs-tx-poll-until-confirmed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Wait for a transaction to confirm with timeout + CI-friendly exit codes.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run src/tx-poll-until-confirmed.test.ts",
    "test:testnet": "vitest run src/tx-poll-until-confirmed.testnet.test.ts"
  },
  "dependencies": {
    "@klever/connect": "^0.1.7"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
