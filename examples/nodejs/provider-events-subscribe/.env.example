# Network. WebSocket events are typically only available on the local network.
# On testnet/mainnet the SDK falls back to internal polling.
KLV_NETWORK=testnet

# Run for this many ms before exiting (useful for demos / CI). Default 20000.
RUN_FOR_MS=20000
