{
  "name": "@klever/example-nodejs-freeze-for-staking",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Freeze KLV (or a KDA) for staking. Returns a bucketId you can later delegate / unfreeze.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run src/freeze-for-staking.test.ts",
    "test:testnet": "vitest run src/freeze-for-staking.testnet.test.ts"
  },
  "dependencies": {
    "@klever/connect": "^0.1.7"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.0"
  }
}
